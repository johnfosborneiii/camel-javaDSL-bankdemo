Camel Router Project for Java DSL 
=========================================

To build this project use

    mvn install

To run the project you can execute the following Maven goal

    mvn exec:java -Dexec.mainClass=org.apache.camel.demos.bankdemo.bankTransactionJavaDSL
